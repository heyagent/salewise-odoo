<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="salewise_account_payment_term_kanban" model="ir.ui.view">
        <field name="name">salewise.account.payment.term.kanban</field>
        <field name="model">account.payment.term</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <kanban>
                <templates>
                    <t t-name="card">
                        <field name="id"/>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>