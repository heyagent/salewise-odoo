<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="salewise_contract_terminate_reason_search" model="ir.ui.view">
        <field name="name">salewise.contract.terminate.reason.search</field>
        <field name="model">contract.terminate.reason</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <search>
                <field name="id"/>
            </search>
        </field>
    </record>
</odoo>