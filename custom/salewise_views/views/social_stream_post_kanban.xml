<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="salewise_social_stream_post_kanban" model="ir.ui.view">
        <field name="name">salewise.social.stream.post.kanban</field>
        <field name="model">social.stream.post</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <kanban>
                <templates>
                    <t t-name="card">
                        <field name="id"/>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>